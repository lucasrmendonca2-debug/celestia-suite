# ============ Discord OAuth (dashboard) ============
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
# URL pública do dashboard (sem barra no fim). Em dev: http://localhost:8080
APP_URL=http://localhost:8080

# ============ Sessão ============
# Gere com: openssl rand -hex 32
SESSION_SECRET=

# ============ Bot (mesmas chaves usadas em bot/.env) ============
DISCORD_TOKEN=
BOT_OWNER_ID=
MONGO_URI=
